name: K6 Load Test

on:
  push:
    branches: [ "feat/swagger-tester" ]

jobs:
  k6-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run k6
        run: |
          k6 run k6/team-load-test.js k6/match-load-test.js
        env:
          TOKEN: ${{ secrets.TOKEN }}
